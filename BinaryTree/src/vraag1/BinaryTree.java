package vraag1;

import java.util.Stack;

public class BinaryTree<E extends Comparable<E>> extends AbstractTree<E>{
	
	
	
	public int inOrderTraversal(int input){
		// INORDER
		int current = root; // Begin bij de root
		while ( current is not NULL or stack is nonempty )[1]
		   if ( current is not NULL ){
		  	// zelf aanvullen, 2 regels
		   }
		   else {
		  	// zelf aanvullen, 3 regels
		 
		   }
	}

	public int preOrderTraversal(int input){
		
		// PREORDER
		current = root; // Begin bij de root
		while ( current is not NULL or stack is nonempty )
		   if ( current is not NULL ){
		  	// zelf aanvullen, 3 regels
		   }
		   else {
		  	// zelf aanvullen, 2 regels
		   }

	}
	
	public int postOrderTraversal(int input){
		
		// POSTORDER
		current = root; // Begin bij de root
		while ( current is not NULL or stack is nonempty )
		   if ( current is not NULL ){
		  	// zelf aanvullen, 2 regels
		   }
		   else {
		  	current = pop
		  	if ( current.secondPop ){
		     	// zelf aanvullen, 2 regels
		  	}
		  	else{
		     	// zelf aanvullen, 3 regels
		  	}
		   }
	}
}
